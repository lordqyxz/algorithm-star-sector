---
title: 分治法动画
aliases:
  - 分治法可视化
  - 分治法课堂动画
tags:
  - 学习/算法
  - 学习/算法/分治法
  - 学习/算法/排序
---

# 分治法：先拆开，再合回来

> [!abstract] 给学生的第一句话
> 分治法不是一条难背的公式，而是一种做事顺序：把大问题拆成小问题，分别解决，再把小答案合成大答案。

```html-embed
path: animations/divide-and-conquer-classroom.html
height: 1200
```

## 动画一：归并排序的递归树

动画一用 $n=8$ 的归并排序，把递归树、每层工作量和公式绑在一起：

1. **Divide｜拆分**：一个规模 8 的问题变成两个规模 4，再继续变成规模 2、1。
2. **Conquer｜解决**：到达基例后，单个数字天然有序。
3. **Combine｜合并**：从两个有序小组的队首取较小者，向上合成大答案。
4. **复杂度**：每层处理 8 个数字，共 $log_2 8=3$ 个合并层，所以总工作量是 $8+8+8=3\times8=\Theta(n\log n)$。

## 为什么会出现 $n\log n$

归并排序中，规模为 $n$ 的一层合并总共只处理约 $n$ 个数字；规模从 $n$ 减半到 $1$ 需要 $\log_2 n$ 层。因此：

$$
\text{总工作量}\approx n\times\log_2 n.
$$

这里要让学生看到：$n$ 变大时，一方面每层工作量变大，另一方面只增加对数级的层数，而不是把所有元素两两暴力比较。动画中的“下一步”会同时更新递归树、公式和右侧数字，避免把公式当成脱离图形的字幕。

## 动画二：最近点对

最近点对是同一套分治思想的几何版本：点集先按横坐标分成左右两半，各自递归求最近距离，再检查中线附近的条带，因为真正的全局答案可能跨过分界线。

切换到动画二后，用 8 个具体点展示四个关键数字变化：

- 左半边得到 $\delta_L\approx3.16$，右半边得到 $\delta_R\approx3.16$；
- 先取局部最好值 $\delta=\min(\delta_L,\delta_R)=3.16$；
- 因为跨界点若想更近，就必须落在中线两侧宽度为 $\delta$ 的条带里；
- 条带中的 $C$、$E$ 距离为 $2$，因此推翻局部答案，得到全局最近点对 $C$–$E$。

它与主方法的关系是：最近点对先提供一个“递推式来自哪里”的具体算法，之后再用主方法分析

$$
T(n)=2T(n/2)+O(n)\Rightarrow\Theta(n\log n)。
$$

> [!warning] 不要把“分治法”和“递归”画等号
> 递归只是常见的实现方式。分治法的关键是三个环节同时成立：子问题更小、子问题可分别解决、子问题结果能合并回原问题。

页面通过已启用的 `Local HTML Embed` 插件加载一个课堂网页，课堂网页内部上下排列两个独立动画。如果 CDN、动画脚本或内嵌窗口不可用，页面仍提供文字说明和两个单独打开的链接：[divide-and-conquer-classroom.html](./animations/divide-and-conquer-classroom.html) · [divide-and-conquer.html](./animations/divide-and-conquer.html) · [closest-pair.html](./animations/closest-pair.html)。

关联：[[分治法]] · [[复杂度分析]] · [[主方法动画]] · [[分治法动画#动画二：最近点对|最近点对动画]]
