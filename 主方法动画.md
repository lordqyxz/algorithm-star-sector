---
title: 主方法动画
aliases:
  - 主方法可视化
  - 分治法主方法动画
tags:
  - 学习/算法
  - 学习/算法/分治法
  - 学习/算法/复杂度
---

# 主方法：把数字画出来

> [!abstract] 先看一个具体数字
> 不要先背“规则 1、2、3”。先看 $n=16$ 的递归树：每一层有多少个问题、每个问题做多少工作、整层加起来是多少。

```html-embed
path: animations/master-method.html
height: 980
```

## 这次动画实际展示什么

- **实验 01｜画出递归树**：对 $T(16)=2T(8)+16$，逐层显示
  $1\times16,\ 2\times8,\ 4\times4,\ 8\times2,\ 16\times1$，每层总工作量都是 $16$。
- **实验 02｜改变 $f(m)$**：保持 $a=2,b=2,n=16$ 不变，只切换 $f(m)=1,m,m^2$，让层工作量的柱状图和表格实时变化。
- **实验 03｜看谁主导**：把三组数字并排相加：$1+2+4+8+16$、$16+16+16+16+16$、$256+128+64+32+16$，直接看到叶子、所有层、根部三种主导模式。

> [!tip] 推荐课堂顺序
> 先用 [[分治法动画]] 的动画一看“递归树为什么长出来”，再用动画二看一个跨界候选如何改变局部答案，最后回到本页把每层数字压缩成 $W_i=a^i f(n/b^i)$。这样主方法讲的是“如何分析已经写出的递推式”，而不是凭空背三条规则。

> [!note] 充分性检查
> 就主方法本身的数学机制而言，本页已经覆盖：递推式、整层代价、相邻层比例 $r$、三种主导情形和课件例题。它解决的是“结论为什么是这些阶数”；分治法与最近点对页面补上的是“递推式为什么会这样写、合并成本从哪里来”。

## 数学本质（静态版）

对标准递推式：

$$
T(n)=aT(n/b)+f(n)
$$

第 $i$ 层有 $a^i$ 个子问题，每个子问题的规模是 $n/b^i$，因此整层工作量是：

$$
W_i=a^i f(n/b^i).
$$

若 $f(m)=m^p$，且 $k=\log_b a$，则：

$$
W_i=a^i(n/b^i)^p
=n^p\left(\frac{a}{b^p}\right)^i
=n^p\left(b^{k-p}\right)^i.
$$

也就是说，真正决定结论的是相邻两层的比例：

$$
r=\frac{a}{b^p}=b^{k-p}.
$$

| 数字关系 | 图形变化 | 求和结果 |
| --- | --- | --- |
| $r>1$ | 越往下越长 | 叶子层主导，$\Theta(n^k)$ |
| $r=1$ | 每根柱子一样长 | 每层相加，$\Theta(n^k\log n)$ |
| $r<1$ | 越往下越短 | 根部主导，$\Theta(f(n))$ |

## 和课件例题的关系

- 例 2.5：$9T(n/3)+n\log n$，$k=\log_3 9=2$，属于递归树基准更重的情形，得到 $\Theta(n^2)$。
- 例 2.7：$2T(n/2)+n$，$k=1$，每层同阶，得到 $\Theta(n\log n)$。
- 例 2.8：$3T(n/2)+n^2\log n$，当前层更重，并需检查正则条件，得到 $\Theta(n^2\log n)$。
- 例 2.4：$2T(n/2)+n\log n$ 位于经典三规则的适用间隙，不能直接套用三条规则。

> [!warning] 看图时不要漏掉“整层”
> 递归树不是只看一个子问题的 $f(m)$。必须先把“这一层有多少个问题”乘进去，得到 $W_i$，再比较各层总量。

页面通过已启用的 `Local HTML Embed` 插件加载本目录下的 `animations/master-method.html`。如果浏览器、CDN 或动画脚本不可用，本文的数字、表格和公式仍可独立阅读；浏览器单独打开：[master-method.html](./animations/master-method.html)。

关联：[[分治法]] · [[复杂度分析]]
