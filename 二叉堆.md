---
title: 二叉堆
tags:
  - 学习/算法
  - 学习/算法/数据结构
---

# 二叉堆

二叉堆是用完全二叉树表达的堆结构，常用于实现优先队列。它的核心不是维持全局有序，而是维持父节点与子节点之间的堆序关系。

## 两个条件

1. **完全二叉树**：除最后一层外均填满，最后一层从左到右连续排列。
2. **堆序性质**：最小堆中父节点不大于子节点；最大堆中父节点不小于子节点。

完全二叉树通常用数组保存。若下标从 $0$ 开始，节点 `i` 的孩子下标为 `2i+1` 和 `2i+2`，父节点下标为 $\lfloor(i-1)/2\rfloor$（根节点除外）。

## 基本操作

| 操作 | 典型复杂度 | 说明 |
| --- | --- | --- |
| 查看最小值或最大值 | $O(1)$ | 位于根节点 |
| 插入 | $O(\log n)$ | 向上调整 |
| 删除根节点 | $O(\log n)$ | 末元素补位后向下调整 |
| 建堆 | $O(n)$ | 自底向上建堆 |

因此，二叉堆是实现优先队列的高效结构，但它不支持像有序数组那样的任意顺序访问。

## 关联

- [[树]]：二叉堆的结构基础是完全二叉树。
- [[线性表]]：堆通常以数组这一线性存储形式实现。
- [[复杂度分析]]：解释调整路径为何与树高相关。
- [[算法知识图谱]]：数据结构主线。
