---
title: 复杂度分析
tags:
  - 学习/算法
  - 学习/算法/基础
---

# 复杂度分析

复杂度分析关注：当输入规模增长时，算法需要多少时间和额外空间。它不追求某台机器上的精确秒数，而是比较增长趋势。

## 输入规模

### 模型 1：元素数量

把输入中的元素个数 $n$ 作为规模，例如线性表含有 $n$ 个元素。

### 模型 2：编码长度

对整数、字符串或图等对象，也可以用输入数据的二进制表示长度作为规模。选择模型时，要说明一个基本操作实际能处理的数据量。

## 增长阶

- $O(1)$：成本与 $n$ 无关，例如数组按下标访问。
- $O(\log n)$：每次操作显著缩小搜索范围。
- $O(n)$：通常需要扫描全部元素。
- $O(n\log n)$、$O(n^2)$：来自分层处理或嵌套遍历等结构。

无法在单个基本操作时间内完成的工作，不能被不加说明地当作一步。

## 情况与资源

- **最好情况**：输入最有利时的成本。
- **最坏情况**：输入最不利时的成本，通常用于判断算法能否满足时限。
- **平均情况**：需要明确输入分布，不能把“平均”当作无条件保证。
- **空间复杂度**：除输入本身外，关注辅助数组、递归栈和缓存等额外空间。

## AI 算法的反例意识

分析 AI 算法时，除了给出典型输入，也要主动寻找会让时间、空间或效果恶化的反例；这能帮助区分经验表现与复杂度保证。

## 关联

- [[导论]]：为什么复杂度是算法与数据结构的共同尺度。
- [[线性表]]：比较数组、链表、栈和队列的操作成本。
- [[树]]：进入树结构及其专门实现。
- [[递归调用]]：分析递归深度和调用次数。
- [[分治法]]：用递推关系估算分治成本。
- [[算法知识图谱]]：回到总览。
