# 前端游戏引擎选型调研（2026-09）与结论

> 结论一句话：瓶颈是"缺一层引擎式抽象"，不是"缺一个引擎"。不引入现成引擎；把内部
> trace/命令日志层升级为游戏框架（`src/game-framework/`），PixiJS 留作性能逃生通道。

## 一、候选对比（摘要）

| 引擎 | TS | LLM 语料 | React 集成 | 包体 gzip | 维护 | 场景适配 | 点评 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Phaser 4 | 附类型 | 5（多为 v3） | 命令式挂载 | 347KB | 活跃 | 2 | v4 刚重写渲染器，语料 v3 主导=幻觉高发；canvas 文本毁无障碍 |
| PixiJS v8 | 好 | 4 | @pixi/react 声明支持 React19 | 252KB | 活跃 | 3 | 性能天花板高，但几十个格子用不到批渲染 |
| Excalibur | 最好 | 2 | 弱 | ~200KB | 一般 | 3 | pre-1.0、语料少，LLM 幻觉风险大 |
| KAPLAY | 一般 | 2 | 弱 | ~150KB | 一般 | 2 | kaboom/kaplay 双版本线互相污染 |
| MelonJS | 已 TS | 2 | 弱 | ~200KB | 活跃 | 2 | Tiled/平台跳跃基因，与网格回合制错位 |
| Cocos Creator | 一般 | 2 | 差 | 外置 | 活跃 | 1 | 编辑器工作流与代码仓库冲突 |
| PlayCanvas v2 | 一般 | 2 | 弱 | ~200KB | 活跃 | 2 | 生态绑定在线编辑器 |
| Three/Babylon | 好 | 5/3 | 好 | 181KB/4MB | 活跃 | 1 | 3D 定位过度 |
| Godot 4 Web | ✗ | 2 | iframe 隔离 | 数十 MB wasm | 活跃 | 1 | 线程版强制 COOP/COEP，与 React 站互斥 |
| DOM/SVG 自绘（现状） | 5 | 5 | 5 | 0 | — | 4 | 缺的是抽象层，不是渲染能力 |

## 二、三条路线的裁决

1. **整体采用引擎重写**：否——canvas 承载 UI 违反"语义控件/键盘可达/无 JS 可读"红线；双份状态真相；LLM 语料错位。
2. **只引入 PixiJS 渲染层**：不划算——单屏几十个实体够不着 WebGL 收益阈值，换来 −252KB 与无障碍归零。留作逃生通道。
3. **升级自有引擎为内部游戏框架（采用）**：React 状态即游戏状态；DOM/SVG 保留可达性与语料优势；补四层薄抽象。

## 三、采用的框架蓝图（src/game-framework/）

| 层 | 职责 | 实现 |
| --- | --- | --- |
| 舞台/精灵（Stage/Actor） | 身份稳定的格位实体；位置即数据，DOM 节点跨帧持久 | `stage.tsx`：绝对定位 actor + CSS transform 补间 |
| 补间（Tween） | 位置/外观过渡；reduced-motion 自动降级 | CSS transition（anime.js 已有依赖，用于入场效果） |
| 输入（Input） | 键盘快捷键 + 指针；语义控件不退化 | `keys.ts`：useGameKeys 映射 |
| 音频（Audio） | 零资产合成音效；可静音、懒初始化 | `audio.ts`：WebAudio 振荡器包络 |
| 镜头（Camera） | 世界地图扩大后的平移/缩放 | 预留接口，暂无消费者不实现（YAGNI） |

要点：精灵 = 元素身份（cell id）跨帧保持、位置变化自动补间——这正是方法论"移动真实数字"的引擎化落地。

来源：Phaser/PixiJS/Excalibur/KAPLAY/melonJS/PlayCanvas/Three/Babylon 各自 GitHub releases、@pixi/react、Godot Web 导出文档、bundlephobia、GitHub topic:html5-game（详见调研对话记录）。
