---
title: 线性表
tags:
  - 学习/算法
  - 学习/算法/数据结构
---

# 线性表

线性表是 $n$ 个元素组成的有限序列。元素之间存在一对一的前后关系，首元素没有前驱，尾元素没有后继。

## 两种常见实现

| 实现 | 存储特点 | 典型优势 | 典型代价 |
| --- | --- | --- | --- |
| 数组 | 连续存储 | 按下标访问通常为 $O(1)$ | 中间插入、删除通常需要移动元素 |
| 链表 | 通过链接组织节点 | 已知位置时插入、删除方便 | 按位置访问通常需要 $O(n)$ 遍历 |

实际成本取决于操作、实现和是否已经持有目标位置；不要只用“数组快、链表慢”概括。

## 栈

栈是后进先出（LIFO）的线性表。

- `push`：入栈。
- `pop`：出栈。
- `peek`：查看栈顶。

常用数组或链表实现；在容量和扩容策略明确时，基本操作可以达到摊销 $O(1)$。

## 队列

队列是先进先出（FIFO）的线性表。

- `enqueue`：入队。
- `dequeue`：出队。
- `front`：查看队首。

用数组实现时，通常需要循环队列或首尾指针，避免每次出队都整体移动元素。

## 关联

- [[复杂度分析]]：比较不同实现的操作成本。
- [[树]]：从线性关系转向层级关系。
- [[算法知识图谱]]：数据结构主线。
